# Claude Code CLI PR50 two-chat evidence

Claude Code CLI 2.1.263 was run with the requested Sonnet 5 model at medium effort under a fresh native `auto` permission configuration. The invocation was restricted, used an empty MCP configuration, disabled browser and agents, disabled session persistence, and allowed Read/Write/Edit/Glob/Grep plus Bash scoped to the Apparatus CLI. Both archived JSON results report an empty `permission_denials` array.

The first chat ran for 57.32 seconds. It produced the accurate workshop plan, saved and verified the sourced Memory fact, registered and carded the Library source, and completed its snapshot. The existing managed state changed only as expected; a temporary helper stayed inside the project and was removed.

The second fresh chat ran for 49.122 seconds as no-save. It recalled the Memory and Library evidence, wrote the cited facilitator note with duration left unknown, and produced only new no-save task metadata plus the deliverable. The independent before/after filesystem oracle found all pre-existing files unchanged.

The canonical produce-deliverable Skill read and the semantic/persisted outcomes are explicit in the archived event stream. Telemetry reports Sonnet 5 plus a Haiku 4.5 helper, while subagent statistics report none spawned; this evidence does not call that a subagent. Prior failed Claude runs elsewhere remain relevant. No global permission, provider-retention, signing, public-release, or full-certification claim is made.

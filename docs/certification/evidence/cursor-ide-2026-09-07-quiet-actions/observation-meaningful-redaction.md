Native Cursor ran on Sep 7, 2026, at 12:26 PM Pacific for 51 seconds. The final reported that the Apparatus credential floor replaced one password value from the exact --from-file input and named the fact, redaction receipt, and snapshot a12c1d5050ae. Actual add-fact stdout was not expanded; this command-result wording is final-reply evidence. Root verified the unchanged synthetic input hash, sanitized stored fact, and receipt class/count without the original value, plus frozen before/after hashes. The raw input value is excluded from public evidence; it is deliberately fake operator fixture data. The snapshot captured managed state only.

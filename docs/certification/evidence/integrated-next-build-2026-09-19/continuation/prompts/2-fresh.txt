Please pick up the Harbor worksheet where we left off and turn the outline into `participant-worksheet.md`. Use the earlier delivery decision and current brief, cite the sources with relative Markdown links, and leave genuinely missing details visible. Keep the worksheet useful for the stated exercise without inventing an equipment report.

This continuation does not save Memory. For Apparatus commands use `<EXECUTABLE>` with existing no-save task `<TASK>` in linked work area `<WORKAREA>`. The requested project document may be saved; preserve every existing file and do not change Memory, Library, goals, task controls, receipts, snapshots or caches. Work only in this synthetic project and its explicitly linked work area. Use one assistant, no subagents, web, MCP, external model CLIs, or user/global Skills. Report native tool or permission denial without bypassing it.

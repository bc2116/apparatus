Review the Harbor brief and save a short `outline.md` for the participant worksheet, citing its source. We have decided to use a two-page printed worksheet so the session works without internet or a display. Remember that decision and its reason, dated 2026-09-19, so we can continue later. Bring the existing worksheet goal's next action up to date, and add `project/brief.md` to the Library. Keep the undecided details open and preserve existing files.

This is a saving task. For Apparatus commands use `<EXECUTABLE>` with existing task `<TASK>` in linked work area `<WORKAREA>`. Work only in this synthetic project and its explicitly linked work area. Use one assistant, no subagents, web, MCP, external model CLIs, or user/global Skills. Report native tool or permission denial without bypassing it.

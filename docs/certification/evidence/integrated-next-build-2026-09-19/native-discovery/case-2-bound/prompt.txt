Open only this independently bound synthetic project. Create
`bound-witness-note.md` from `bound-task-source.md`. Keep it concise, separate
confirmed work from scheduled work, and cite the source with a relative Markdown
link. Preserve all existing files. The project’s explicit work-area link is
`.apparatus/workspace.yaml`; use that link if work-area context is needed. This
is a synthetic, no-save test: do not save Memory, create learned Skills, or make
automatic cards or activity notes. Work only in this locally opened project and
its explicitly linked work area. If an Apparatus task control is needed, continue
only with task `<BOUND_NO_SAVE_TASK_ID>` for work area
`<FIXTURE_ROOT>/case-2-bound/workarea`. Use one assistant; do not use subagents,
web, MCP, external model CLIs, or user/global Skills. Stop and report any native
permission or tool denial without trying to bypass it.

Use `<EXECUTABLE>` for every Apparatus command; this is the declared built runtime for this probe.

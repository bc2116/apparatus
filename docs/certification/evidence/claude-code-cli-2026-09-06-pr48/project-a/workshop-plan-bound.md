# Practice Workshop Plan (Bound)

Source: `project-a/source.md`

## Sessions

- Two sessions, 12 places each (24 places total) (`source.md:3`).
- A water break comes after 45 minutes into each session — the source gives
  the break's *timing*, not its duration; how long the break itself lasts is
  not stated (`source.md:3`).

## Venue

- Undecided. The source explicitly leaves the venue unresolved, so do not
  schedule around one until it is confirmed (`source.md:3`).

## What the source does not establish

- Venue/location (only that it is undecided).
- Break duration (only that it starts after the 45-minute mark).
- Session length beyond the 45-minute mark before the break (total duration,
  start/end times).
- Dates or times.
- Topic/curriculum content, facilitator(s), or materials needed.
- Registration process or audience/eligibility for the 12 places per session.

## Note on this bound copy

This file is the finished version prepared in the bound project area. It
preserves the previous plan (`workshop-plan.md`), `source.md`, `local-note.md`,
and this project's custom instructions (`AGENTS.md`) unchanged.

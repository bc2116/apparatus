# Cursor IDE Luna evidence

Cursor IDE 3.19.13 on macOS 27 used GPT-5.6 Luna Medium in two fresh native conversations on candidate `964d5a1`, with the default cache. The first conversation lasted 52 seconds. It created the sourced workshop plan, saved and verified the Memory fact, added the Library card, preserved existing files, and was independently checked by the root operator. Native UI displayed use of the exact canonical `apparatus-produce-deliverable` Skill path; this was prompted Skill use, not automatic discovery.

The fresh second conversation lasted 79 seconds. It recalled the Memory and Library evidence with citations and created the requested facilitator note. Independent root verification found the note accurate and found all 85 pre-existing files byte-identical; only the requested note and content-free no-save task metadata were added.

The final answer accurately said the venue was undecided and total duration was unspecified. The source's timing is a water break after 45 minutes, not a 45-minute break; the preserved note keeps that distinction. No native denial was observed. This supports a pass for this named Cursor/Luna two-chat case.

Native UI observations were operator-supplied rather than a verbatim transcript or complete hidden tool trace. The case does not generalize to other Cursor models, provider retention, OS read confinement, signing, public release, installer install-and-repair. Together with the separately recorded Codex CLI and Claude Code CLI pairs, it completes the focused app-chat gate for the named configurations; installer acceptance remains separate.

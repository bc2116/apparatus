# Practice workshop plan

Reusable run-of-show from `project-a/source.md`. Source facts, unknowns, and a reuse proposal are kept distinct. Claims below cite that source. Details not in the source are listed as missing and are not filled in.

## Source facts

These items are stated in `project-a/source.md`. They are not a session template.

- Format: a practice workshop (`project-a/source.md`).
- Sessions: **two** (`project-a/source.md`).
- Capacity: **12 places each** session (`project-a/source.md`).
- Timing: allow a **water break after 45 minutes** (`project-a/source.md`). The source does not say whether that break applies once per session or once across the workshop.
- Venue: **undecided** (`project-a/source.md`).

## Proposed reusable session sequence

The numbered sequence below is a **proposal** for reuse. It is not established by the source. The 12-place cap and two-session count are source facts. Placing the 45-minute water break inside each session is an assumption, not a source fact.

1. Open the session with the 12-place cap in force.
2. Run the first 45 minutes, then take the water break.
3. Resume after the break and close the session.
4. Repeat for the second session.

Do not change the 12-place cap or the two-session count unless a later source replaces them. Treat the per-session water-break placement as a proposal until a later source or owner decision settles whether the 45-minute break applies per session or across the workshop.

## Unknowns

The source does not specify:

- whether the 45-minute water break applies per session or across the workshop
- venue or room (explicitly undecided)
- date, start time, or time zone
- total session length after the 45-minute break
- whether the two sessions are the same day, consecutive, or separate
- audience, facilitators, or helpers
- agenda, learning objectives, or activities
- materials, equipment, or accessibility needs
- registration, waitlist, or overflow rules
- how the water break is hosted (location, duration, catering)

Leave those fields blank until a later source or owner decision supplies them.

## Reuse checklist

- [ ] Confirm the two-session, 12-places-each limits still apply
- [ ] Keep the source fact of a water break after 45 minutes; do not treat per-session vs workshop-wide placement as settled
- [ ] Record the venue once it is decided
- [ ] Fill only the missing fields above from a cited source; do not invent them

Operator revision: confirm the venue with the organizer.

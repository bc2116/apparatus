# Root named invocation — qualified observation

Observed September 18, 2026, approximately 21:28 America/Los_Angeles, in a new
local Cursor IDE 3.21.13 chat, GPT-5.6 Luna Medium, macOS 27.0 arm64.

The computer-control paste operation timed out and accessibility set-value
left the composer empty. Its native typing fallback submitted the prompt's
lines as successive messages. Thus the recorded prompt text reached one fresh
chat incrementally, not as the protocol's single initial prompt. This is a
**partial** protocol result, not a clean pass. No replacement run was attempted.

Cursor displayed `Used apparatus-native-discovery-witness`. Opening that entry
resolved the editor to the canonical work-area
`.agents/skills/apparatus-native-discovery-witness/SKILL.md`, whose inspected
body and hash match the prepared fixture. The persisted `root-witness-note.md`
contains the unique body marker, distinguishes the completed documentation
check from the scheduled accessibility review, preserves the unknown release
date, and links the source. This supplies native invocation plus body-use
 evidence for this qualified named case, not automatic relevance discovery.

The native task-status command could not find the Apparatus executable. Task
selection was therefore not validated by that command. Independent before/after
hashes show all 60 pre-existing files unchanged and only the requested note
added. No Memory or other managed content was added. This preservation result
does not establish that a task-control command succeeded.

See [hashes](hashes.json) and [saved note](root-witness-note.md). The expected
prompt is [root named invocation](../../prompt-1-root-named.txt); the placeholder
values identified only the synthetic fixture location and its prepared no-save
control. No transcript, conversation identifier or personal path is retained.

# Independent bound-project relevance case — unavailable

Observed September 18, 2026, approximately 21:33 America/Los_Angeles, in a fresh
local Cursor IDE 3.21.13 chat with GPT-5.6 Luna Medium on macOS 27.0 arm64.
The opened `project-bound/` folder was its own Git root, explicitly bound to
its parent work area. The intact [prompt](../../prompt-2-bound-relevance.txt)
named neither the Skill nor its body marker.

Cursor stopped after its required `apparatus` task-status command was unavailable
on the native command path. The fixture used repository tooling for setup but
did not provide an executable path in this discovery prompt. This is a fixture
prerequisite gap, not evidence that the app cannot discover a bound Skill and
not a product-installer failure. No native invocation or canonical-body outcome
was established. No retry or changed prompt was used.

Independent [hashes](hashes.json) show all 66 pre-existing work-area files
(excluding project Git internals) unchanged, including the canonical witness
and custom project Skill. The requested output is absent. Retain the plain-file
fallback; there is insufficient evidence for an adapter decision.

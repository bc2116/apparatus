# Codex CLI memory-gap evidence

Codex CLI 0.153.4 was exercised with `gpt-5.6-terra`, first chat at medium effort and a separate first-chat run at high effort. Both first chats created a workshop plan and Library card but failed the required Memory fact: no `Memory/Facts` entry and no add-fact call. This is a failed Memory-continuity result, not RC compatibility.

The medium-effort fresh second chat was no-save. It produced only task metadata and the requested facilitator note, reported a truthful Memory no-match, and could not demonstrate Memory continuity. The high-effort run had no second chat. An initial symlink-launcher companion failure preceded the vendor-binary retry and remains an environment gap.

The curated prompts use `<fixture-root>` and `<fixture-runtime>` tokens. Finals and the small plan output are normalized from explicit run artifacts. Raw provider JSONL is excluded. No claim is made about unobserved skill execution, global permissions, provider retention, signing, public release, or full certification.

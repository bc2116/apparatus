Native Cursor ran on Sep 7, 2026, at 12:16 PM Pacific for 21 seconds. The expanded exact snapshot command used an empty PATH only for that process; the native output said snapshots were unavailable and to run doctor. The final reported exit 1, no recovery point, the existing saving task retained, and doctor as the concrete next action because this test had Git off PATH. No retry, installation, global PATH change, or additional snapshot was observed. A separate task-status read preceded the scoped snapshot, so this is not evidence of literally one total shell command. Full area/cache deltas were frozen for verification.
